# Catalytic 用户操作手册

## 目录

1. [系统概述](#1-系统概述)
2. [设置页面概览](#2-设置页面概览)
3. [设备管理](#3-设备管理)
4. [槽位绑定](#4-槽位绑定)
5. [测试流程设置](#5-测试流程设置)
6. [导入/导出配置](#6-导入导出配置)
7. [常见问题解答](#7-常见问题解答)

---

## 1. 系统概述

Catalytic 是一套高可靠自动化测试数据采集调度系统，用于连接测试仪器、执行测试流程、判定测试结果。本系统采用可视化配置方式，无需编写代码即可完成常规测试流程的定义与执行。

### 主要功能

- **设备管理**：定义设备类型、添加设备实例、配置通讯命令
- **槽位绑定**：将设备实例绑定到测试槽位，支持多设备并行测试
- **测试流程**：定义测试步骤、执行逻辑、判定规则
- **配置管理**：导入/导出测试配置，便于在不同工位间迁移

---

## 2. 设置页面概览

点击界面右上角的 **设置** 按钮进入设置页面。设置页面左侧为导航栏，包含以下选项：

| 选项 | 功能说明 |
|------|----------|
| **设备管理** | 管理设备类型、设备实例和预设命令 |
| **槽位绑定** | 配置每个测试槽位使用的设备 |
| **测试流程** | 定义测试步骤和判定逻辑 |
| **导入/导出** | 导入或导出配置文件 |
| **插件管理** | 查看已加载的插件信息 |
| **连接设置** | 配置与 Host 服务的连接 |
| **关于** | 查看系统版本信息 |

完成配置后，点击右下角的 **保存** 按钮保存所有设置。

---

## 3. 设备管理

设备管理是配置测试系统的基础。您需要先定义设备类型，然后在该类型下添加具体的设备实例。

### 3.1 设备类型的概念

**设备类型** 是一类设备的抽象定义，例如：
- DMM（数字万用表）
- PowerSupply（电源）
- OSC（示波器）
- DUT（被测设备）

每个设备类型包含：
- **通讯插件**：指定使用哪种通讯方式（如串口、TCP/IP）
- **设备实例**：具体的物理设备（如"DMM-001"、"DMM-002"）
- **预设命令**：该类型设备支持的指令集

### 3.2 添加设备类型

1. 在设备管理页面底部，点击 **"+ Add Device Type"**
2. 在弹出的对话框中填写：
   - **类型名称**：如 "DMM"、"PowerSupply"
   - **通讯插件**：从下拉列表选择通讯方式
     - 串口通讯：选择包含 "serial" 的插件
     - 网络通讯：选择包含 "tcp" 或 "socket" 的插件
   - **共享设备类型**：勾选表示此类型设备可被多个槽位共用
3. 点击 **Add** 确认

### 3.3 展开设备类型详情

添加设备类型后，点击类型名称行可展开详情面板，显示：
- **设备列表**：该类型下的所有设备实例
- **预设命令**：该类型设备支持的指令

### 3.4 添加设备实例

1. 展开目标设备类型
2. 点击类型名称右侧的 **"+"** 按钮
3. 在对话框中填写：
   - **设备名称**：如 "DMM-001"、"Power-01"
   - **设备地址**：
     - 串口设备：如 `COM3`、`/dev/ttyUSB0`
     - 网络设备：如 `192.168.1.100:5025`
4. 点击 **Add** 确认

### 3.5 配置预设命令

预设命令定义了该类型设备可执行的指令，方便在测试流程中快速调用。

#### 添加命令

1. 展开设备类型，找到 "预设命令" 区域
2. 点击 **"+ 添加命令"**
3. 填写命令参数：

| 参数 | 说明 | 示例 |
|------|------|------|
| **命令名称** | 命令的描述性名称 | 读取电压 |
| **命令内容** | 发送给设备的指令 | `MEAS:VOLT:DC?` |
| **解析规则** | 从响应中提取数据的正则表达式（可选） | `([0-9.]+)` |
| **超时时间** | 等待响应的时间（毫秒） | 1000 |
| **发送结束符** | 命令发送时的结束字符 | LF (`\n`)、CRLF (`\r\n`)、CR (`\r`)、无、自定义 |
| **响应结束符** | 响应接收时的结束字符 | LF (`\n`)、CRLF (`\r\n`)、CR (`\r`)、无、自定义 |

4. 点击 **Add** 确认

#### 编辑命令

点击命令行右侧的编辑图标（✎）可修改命令参数。

### 3.6 编辑和删除

- **编辑设备类型**：点击类型名称右侧的 "Edit" 按钮
- **删除设备类型**：点击 "Delete" 按钮（会同时删除该类型下的所有设备和命令）
- **编辑设备实例**：点击设备名称右侧的 "Edit" 按钮
- **删除设备实例**：点击设备名称右侧的 "Delete" 按钮

### 3.7 设备状态指示

在设备列表中，每个设备左侧有一个状态指示条：
- **绿色**：设备在线/已连接
- **灰色**：设备离线/未连接

---

## 4. 槽位绑定

槽位绑定将设备实例分配给测试槽位，使每个槽位可以独立执行测试。

### 4.1 槽位的概念

**槽位 (Slot)** 是测试执行的独立单元。每个槽位可以绑定多台设备，实现并行测试或多设备协同测试。

例如：
- 槽位 1：绑定 DMM-001 + Power-01
- 槽位 2：绑定 DMM-002 + Power-02
- 槽位 3：绑定 DMM-003 + Power-03

### 4.2 设置槽位数量

在槽位绑定页面右上角，找到 **"总槽位数"** 输入框，输入或修改槽位数量（最小为 1）。

### 4.3 选择槽位

左侧显示所有槽位列表，点击可选中目标槽位，右侧将显示该槽位的绑定详情。

### 4.4 绑定设备到槽位

1. 选中目标槽位
2. 点击右侧的 **"添加绑定"** 按钮
3. 在弹出的对话框中：
   - **步骤 1**：点击选择设备类型（如 DMM）
   - **步骤 2**：从列表中选择具体的设备实例（如 DMM-001）
4. 点击 **确认绑定**

> **注意**：如果步骤 2 显示 "该类型下没有可用设备"，请先在设备管理中添加设备实例。

### 4.5 解除绑定

在槽位详情中，找到要解除的设备，点击右侧的 **"移除"** 链接。

### 4.6 共享设备类型

如果设备类型被标记为 "共享设备类型"，则同一设备实例可以绑定到多个槽位。这对于需要多槽位共用同一台设备的情况非常有用（如公共校准源）。

---

## 5. 测试流程设置

测试流程定义了测试的执行步骤、数据采集方式 and 判定规则。

### 5.1 测试步骤概览

每个测试步骤包含：
- **步骤名称**：步骤的描述性名称
- **执行模式**：标准模式、扩展模式、计算模式
- **执行内容**：根据模式不同而变化
- **变量定义**：从设备响应中提取数据
- **检查规则**：判定步骤结果的条件

### 5.2 添加测试步骤

1. 在测试流程页面底部，点击虚线边框的 **"Add"** 按钮
2. 新步骤会自动展开编辑

### 5.3 步骤基本信息

展开步骤后，可编辑以下信息：

| 字段 | 说明 |
|------|------|
| **名称** | 步骤的描述性名称，如 "读取电压" |
| **标签** | 可选标识符，用于逻辑跳转 |
| **包含在报告中** | 勾选后，此步骤的测试结果计入统计报告 |

### 5.4 执行模式

系统支持三种执行模式：

#### 标准模式 (Standard)

最常用的模式，用于发送命令到设备并解析响应。

**配置项**：
1. **设备类型**：选择要操作的设备类型
2. **执行命令**：选择预设命令（命令列表来自设备管理中定义的预设命令）

#### 扩展模式 (Extension)

用于调用自定义处理器插件执行复杂任务，如数据库交互、复杂算法等。

**配置项**：
1. **任务名称**：从下拉列表选择处理器任务
2. **超时时间**：等待任务完成的超时时间（毫秒）

#### 计算模式 (Calculation)

不发送命令，仅对已有变量进行数学运算。

> **注意**：此功能目前处于开发阶段。

### 5.5 变量定义

变量用于从设备响应中提取数据，供后续判定或其他步骤使用。

**添加变量**：
1. 在 "变量" 区域点击 "Add"
2. 填写：
   - **变量名称**：如 `voltage`、`current`
   - **解析规则**：正则表达式，如 `([0-9.]+)`

**工作原理**：
1. 设备返回响应数据
2. 系统使用解析规则从响应中匹配数据
3. 匹配到的数据存储到变量池
4. 后续步骤可引用此变量

### 5.6 检查规则

检查规则用于判定步骤是否通过。

#### 无检查 (None)

不进行判定，直接通过。

#### 范围检查 (Range Check)

检查变量值是否在指定范围内。

**配置**：
- **变量名称**：选择要检查的变量
- **最小值**：范围下限
- **最大值**：范围上限

**判定逻辑**：`最小值 ≤ 变量值 ≤ 最大值`

#### 阈值检查 (Threshold)

检查变量值是否满足阈值条件。

**配置**：
- **变量名称**：选择要检查的变量
- **操作符**：`<`、`>`、`<=`、`>=`、`==`、`!=`
- **阈值**：比较值

**判定逻辑**：`变量值 操作符 阈值`

#### 包含检查 (Contains)

检查变量值是否包含指定字符串。

**配置**：
- **变量名称**：选择要检查的变量
- **包含内容**：要检查的子字符串

#### 表达式检查 (Expression)

使用正则表达式进行自定义判定。在表达式字段中输入正则表达式，系统会匹配变量值，匹配成功则通过。

**配置**：
- **表达式**：正则表达式，如 `^OK.*` 表示以 "OK" 开头

### 5.7 跳转逻辑

跳转逻辑用于处理异常情况，可配置当失败、超时、错误时跳转到指定步骤。

**配置方式**：
- 在目标步骤上勾选 "失败时跳转"、"超时时跳转"、"错误时跳转"
- 其他步骤出现对应情况时，会跳转到该步骤

**应用场景**：
- 测试失败后跳转到清理步骤
- 超时后跳转到复位步骤
- 错误后跳转到错误处理步骤

### 5.8 步骤排序

使用步骤标题栏右侧的上下箭头可调整步骤顺序。

### 5.9 删除步骤

点击步骤标题栏右侧的删除图标可删除该步骤。

---

## 6. 导入/导出配置

配置的导入/导出功能便于在不同工位或机台间迁移测试配置。

### 6.1 导出配置

1. 进入 **导入/导出** 页面
2. 在 "导出配置" 区域，可看到当前配置的 JSON 预览
3. 点击 **"Save..."** 按钮
4. 选择保存位置和文件名

**导出内容**：
- 设备类型定义
- 预设命令
- 测试步骤
- 槽位数量

> **注意**：设备实例和槽位绑定需要重新配置（因物理地址可能不同）。

### 6.2 导入配置

#### 从文件导入

1. 点击 **"Import from File..."**
2. 选择 JSON 配置文件
3. 系统会显示导入预览，包含导入的步骤数和设备类型数

#### 从剪贴板粘贴

1. 点击 **"Paste JSON to Import..."**
2. 在对话框中粘贴 JSON 配置内容
3. 点击 **Add** 确认

### 6.3 导入后的操作

导入配置后，您需要：
1. 在设备管理中添加设备实例（填写实际地址）
2. 在槽位绑定中将设备绑定到对应槽位
3. 点击 **保存** 保存完整配置

---

## 7. 常见问题解答

### Q1: 为什么测试开始时提示 "请先在设置中配置设备"？

**原因**：系统未检测到已配置的设备。

**解决方法**：
1. 进入设置 → 设备管理
2. 添加设备类型
3. 在设备类型下添加设备实例
4. 点击保存

### Q2: 为什么选择设备类型后，命令下拉列表为空？

**原因**：该设备类型尚未配置预设命令。

**解决方法**：
1. 进入设置 → 设备管理
2. 展开目标设备类型
3. 在 "预设命令" 区域添加命令

### Q3: 为什么槽位绑定时提示 "该类型下没有可用设备"？

**原因**：选中的设备类型下没有添加设备实例。

**解决方法**：
1. 进入设置 → 设备管理
2. 展开目标设备类型
3. 点击 "+" 添加设备实例

### Q4: 设备地址应该怎么填写？

**设备类型** | **地址格式** | **示例**
------------|-------------|---------
串口设备 (Windows) | COM端口号 | COM3、COM5
串口设备 (Linux/macOS) | 设备路径 | /dev/ttyUSB0、/dev/ttyS1
TCP/IP设备 | IP地址:端口 | 192.168.1.100:5025

### Q5: 解析规则怎么写？

解析规则使用正则表达式，需要用括号 `()` 标记要提取的部分。

**示例**：

| 设备响应 | 解析规则 | 提取结果 |
|----------|----------|----------|
| `+1.234E+00` | `([+-]?[0-9.E+-]+)` | 1.234E+00 |
| `VOLT:DC 3.1415` | `VOLT:DC\s+([0-9.]+)` | 3.1415 |
| `OK,5.67,V` | `OK,([0-9.]+)` | 5.67 |

### Q6: 如何在同一台设备上测试多个产品？

1. 在设备管理中，将设备类型标记为 "共享设备类型"
2. 在槽位绑定中，将同一设备实例绑定到多个槽位
3. 注意：由于是共享设备，测试时需注意时序控制

### Q7: 导入配置后为什么设备连接失败？

导入的配置不包含设备实例的物理地址，您需要：
1. 进入设备管理
2. 编辑每个设备实例，填写正确的地址（如 COM 口或 IP 地址）
3. 保存配置

### Q8: 如何查看设备的连接状态？

在主界面右侧的 "设备连接" 面板可查看所有设备的连接状态：
- 绿色圆点：已连接
- 灰色圆点：未连接

点击 "Connect" 或 "Disconnect" 可手动控制设备连接。

---

## 附录：术语表

| 术语 | 英文 | 说明 |
|------|------|------|
| 设备类型 | Device Type | 一类设备的抽象定义 |
| 设备实例 | Device Instance | 具体的物理设备 |
| 槽位 | Slot | 测试执行的独立单元 |
| 预设命令 | Preset Command | 预先定义的设备指令 |
| 解析规则 | Parse Rule | 从响应中提取数据的正则表达式 |
| 检查规则 | Check Rule | 判定测试结果的逻辑 |
| 变量 | Variable | 存储测试数据的命名容器 |
| 通讯插件 | Communicator Plugin | 处理设备通讯的软件组件 |

---

*文档版本：1.0*
*最后更新：2026年4月*
